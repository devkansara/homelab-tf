# Local / secrets - never commit (homelab-tf)

.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info

# Credentials
.env
*/.env
.env.*
!.env.example
*.tfvars
!*.tfvars.example
*.auto.tfvars
*.pem
*.key

# Local-only Nexus overlay (not in public homelab-tf portfolio)
nexus.local.tf
nexus_providers.local.tf
nexus_imports.local.tf
modules/docker-nexus/
modules/proxmox/ct_nexus.tf
modules/proxmox/nexus.local.tf

# OS
.DS_Store
